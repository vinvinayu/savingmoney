name: savingmoneys
description: A Flutter app for managing savings and expenses.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.40.0 # Add this for charting functionality

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Add this line

flutter:
  uses-material-design: true